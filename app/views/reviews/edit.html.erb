<%= render 'form', product: @product, review: @review %>

<%= link_to("Delete", product_review_path(@product.id, @review.id), method: :delete, data: {confirm: "Are you sure you want to delete this review? This cannot be undone!"}) %>
